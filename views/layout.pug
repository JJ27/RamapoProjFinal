
mixin guess_list(guesses, secret)
    ul
        each guess in guesses
            li
                span #{guess.value}
                if guess.value === secret
                    span - Correct!
                else if guess.value < secret
                    span - Too Low!
                else
                    span - Too High!

doctype html
html
    head
        title Contact List
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/water.css@2/out/water.css")
    body
        if !hide_login
            if user
                span
                    span Logged in: #{user.fname} #{user.lname}
                    a(href='/logout')  Logout
            else
                a(href='/login') Login
        block content